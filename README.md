## Voice Recording Disocrd Bot

This project is a Discord bot designed to automatically record voice channel conversations. Built with Node.js and discord.js, it monitors voice state changes to dynamically join channels when users connect and leave when they disconnect. The bot captures audio streams using the @discordjs/voice library and saves the recorded sessions to a local directory, handling multiple concurrent recording sessions across different guilds.
